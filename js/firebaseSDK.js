(function(){
  // Your web app's Firebase configuration
  var firebaseConfig = {
    apiKey: "sorry",
   
  };
  // Initialize Firebase
  firebase.initializeApp(firebaseConfig);
  console.log("intialized")
}());

